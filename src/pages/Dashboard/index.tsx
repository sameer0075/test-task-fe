import Dashboard from "../../Components/Modular-Components/Dashboard"

/**
 * DashboardPage is the main component for the dashboard page.
 * It renders the Dashboard component.
 *
 * @returns {JSX.Element} The rendered DashboardPage component.
 */
const DashboardPage = () => {
  return (
    // Render the Dashboard component
    <Dashboard />
  )
}

export default DashboardPage
